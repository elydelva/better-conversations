services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: better_conv
    ports:
      - "5433:5432"
    volumes:
      - prisma_postgres_data:/var/lib/postgresql/data

volumes:
  prisma_postgres_data:
